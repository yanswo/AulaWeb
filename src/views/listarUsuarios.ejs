<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <ul>
            Lista de Usuários
            <% for(var i = 0; i < dados.length; i++) { %>
                <li>
                    <p>Login : <%= dados[i].login %></p>
                    <p>Senha : <%= dados[i].senha %></p>
                    <p>Permissão : <%= dados[i].permissao %></p>
                </li>
            <% } %>
        </ul>

    </body>
</html>